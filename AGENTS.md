# AGENTS.md

## 1，项目简介

本项目为个人网站，使用原生 HTML、CSS 和 JavaScript。

网站应措辞严谨，风格简约，配色美观，动画顺滑。

## 2，调用回执

在每个新的 Codex 对话或 session 中，第一次开始处理本仓库任务时，首先回复：`AGENTS.md loaded.`

在同一个 Codex 对话或 session 内，后续收到新的任务时不再重复该回执。

回执仅用作确认，不要复述 AGENTS.md 的具体内容。

## 3，代码编辑

### 3.1 代码风格

在任务开始前，先阅读与任务相关的 HTML、CSS 和 JavaScript 代码。

编辑代码时应优先复用已有实现，例如当前项目中已存在的命名方式、缩进方式、空行习惯、注释风格和 CSS 动画实现方式等，保持代码风格一致性。

只修改完成当前任务所必需的代码，不要擅自修改与任务无关的代码、结构或行为。

如果发现与当前任务相关性较弱的问题，可以在结果中指出，不要顺手修改。

保持现有代码的空行和模块分隔习惯，使用 4 个空格缩进，长 CSS 属性不换行。

对于纯视觉和布局行为，应优先考虑使用 CSS 的可能性，若使用 JavaScript，应保持简洁轻量。

### 3.2 注释

优先使用中文注释，涉及属性名、变量名和专有名词时可使用英文。

每个相对独立的小功能模块前应有简短注释，着重解释代码逻辑和非直观行为，目标是维持代码的可读性，为将来可能进行的项目级重构做准备。

对于不容易从代码本身理解的参数或行为逻辑，应增加简短的行内注释。

不要给显而易见的代码逐行添加注释。

### 3.3 Bug 处理

处理 bug 时应基于现有实现做最小侵入修改。

不应为了修复一个局部 bug 大规模重写现有实现，如确有必要进行较大范围调整，应先说明原因和拟采用的解决方案，并向我确认。

修复 bug 后，如果本次修改可能影响某些功能、交互行为或显示效果，请简要列出建议我重点检查的项目。

### 3.4 文件管理

新建文件时应放入正确的位置。

主 HTML 文件位于 `index.html` 。

其他 HTML 文件位于文件夹 `html/` 。

所有 CSS 文件位于文件夹 `styles/` 。

所有 JavaScript 文件位于文件夹 `scripts/` 。

所有图像位于文件夹 `images/` 。

## 4，视觉设计

网站强调简洁、克制和细节。动画应精致而简洁，服务于可读性和美观。

除非任务明确要求，不要擅自修改现有的字体、spacing、breakpoint、配色、border 样式、卡片尺寸和 transition time 等。
